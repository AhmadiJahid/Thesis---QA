# Router Models

Each model folder contains:
- `prompt.md` with the hop-count prompt template
- `config.json` with model and decoding settings

Folder naming uses lowercase with underscores (e.g., `qwen3_1_7b`).
